export type ColorsType = {
  main: string;
  dark: string;
  light: string;
  extra: string;
  background: string;
  color: string;
}

export type ColorsBrightness = {
  foreground: string;
  customProp: string;
};

export type ColorsLowBrightness = {
  white: string;
};

export type ColorsMediumBrightness = {
  shadow: string;
};

export type ColorsHighBrightness = {
  someProp: string;
  anotherProp: string;
  thirdCustomProp: string;
};

export type ColorsUltraBrightness = {
  intensive: string;
};

export type ColorsDepth = {
  background: string;
  foreground: string;
  color: string;
};

export type Colors8BitDepth = {
  borderColor: string;
};

export type Colors16BitDepth = {
  borderColor: string;
  anotherColor: string;
};

export type Colors24BitDepth = {
  extraColor: string;
};
